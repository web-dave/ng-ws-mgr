import { AvailableDirective } from './available.directive';

describe('AvailableDirective', () => {
  it('should create an instance', () => {
    const directive = new AvailableDirective();
    expect(directive).toBeTruthy();
  });
});
